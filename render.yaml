services:
  - type: web
    name: solar-inspection
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        sync: false
      - key: NODE_ENV
        value: production
